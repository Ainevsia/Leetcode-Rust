#include <string>
#include <vector>
#include <iostream>
#include <sstream>
#include <algorithm>
using namespace std;

class Solution {
public:
    vector<vector<int>> subsetsWithDup(vector<int>& nums) {
        
    }
};

int main() {
    Solution a;
    vector<vector<int> > totalset = {{}};
    cout << totalset.size();    // 1 empty set 
}
